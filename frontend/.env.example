# Leave unset for local dev (uses Vite proxy /api → http://localhost:4010)
# VITE_API_URL=https://yourdomain.com

# Auto logout: idle minutes with no activity, max hours per login session
VITE_SESSION_IDLE_MINUTES=30
VITE_SESSION_MAX_HOURS=8
